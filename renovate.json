{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>intechcore/renovate-config"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update the image tag in the README quick start",
      "managerFilePatterns": ["/^README\\.md$/"],
      "matchStrings": [
        "# renovate: image=ghcr\\.io/intechcore/subversion-ldap-httpd\\n\\s*image: ghcr\\.io/intechcore/subversion-ldap-httpd:(?<currentValue>[\\d.]+-\\d+)"
      ],
      "depNameTemplate": "ghcr.io/intechcore/subversion-ldap-httpd",
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "description": "Own image on ghcr.io. Every release publishes the exact build tag (1.14.5-1) next to the moving tag (1.14.5) and latest. Regex versioning tracks only the exact one and orders by the build number. Docker versioning would read -N as a compatibility tag and hide rebuilds.",
      "matchPackageNames": ["ghcr.io/intechcore/subversion-ldap-httpd", "ghcr.io/intechcore/openldap"],
      "versioning": "regex:^(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)-(?<build>\\d+)$"
    }
  ]
}
