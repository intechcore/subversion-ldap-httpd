{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "automerge": true,
  "automergeType": "pr",
  "platformAutomerge": false,
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update the image tag in the README quick start",
      "managerFilePatterns": ["/^README\\.md$/"],
      "matchStrings": [
        "# renovate: image=ghcr\\.io/intechcore/subversion-ldap-httpd\\n\\s*image: ghcr\\.io/intechcore/subversion-ldap-httpd:(?<currentValue>[\\d.]+-\\d+)"
      ],
      "depNameTemplate": "ghcr.io/intechcore/subversion-ldap-httpd",
      "datasourceTemplate": "docker"
    }
  ],
  "hostRules": [
    {
      "matchHost": "ghcr.io",
      "hostType": "docker",
      "username": "itcsrvs",
      "password": "{{ secrets.GHCR_TOKEN }}"
    }
  ],
  "packageRules": [
    {
      "description": "Disable updates for debian:trixie-slim. The codename tag has no version to compare; the weekly Rebuild workflow picks up its rebuilds.",
      "matchDatasources": ["docker"],
      "matchDepNames": ["debian"],
      "enabled": false
    },
    {
      "description": "Own image, private on ghcr.io (see hostRules). Every release publishes the exact build tag (1.14.5-1) next to the moving tag (1.14.5) and latest. Regex versioning tracks only the exact one and orders by the build number. Docker versioning would read -N as a compatibility tag and hide rebuilds.",
      "matchPackageNames": ["ghcr.io/intechcore/subversion-ldap-httpd"],
      "versioning": "regex:^(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)-(?<build>\\d+)$"
    }
  ]
}
