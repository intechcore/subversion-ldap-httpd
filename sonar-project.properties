sonar.projectKey=intechcore_subversion-ldap-httpd
sonar.organization=intechcore
sonar.projectName=subversion-ldap-httpd

# The image ships no repository scripts: its entrypoint is Debian's apache2ctl.
# The shell code in tests/ is the test harness, not product code.
sonar.sources=.
sonar.exclusions=tests/**
sonar.tests=tests
sonar.test.exclusions=tests/integration/fixtures/**
